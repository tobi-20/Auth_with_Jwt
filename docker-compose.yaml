services:
 postgres:
  image: postgres:16-alpine
  container_name: ecom
  environment:
   POSTGRES_USER: xxxxx
   POSTGRES_PASSWORD: xxxxxx
   POSTGRES_DB: name
  ports:
  - "5432:5432"
  volumes:
  - postgres_data:/var/lib/postgresql/data
volumes:

  postgres_data:
